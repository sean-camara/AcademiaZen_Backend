# VAPID Keys for Web Push Notifications
# Generate these using: npm run generate-vapid
# IMPORTANT: Keep the private key secret! Never expose it in frontend code.

VAPID_PUBLIC_KEY=your_public_key_here
VAPID_PRIVATE_KEY=your_private_key_here

# Your email for VAPID identification (required by push services)
VAPID_EMAIL=mailto:your-email@example.com

# Server configuration
PORT=3001

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Optional: multiple allowed frontend origins (comma-separated)
# FRONTEND_URLS=http://localhost:3000,http://localhost:5173

# Optional: allow vercel preview origins
# ALLOW_VERCEL_PREVIEW=false

# Optional: allow requests with no Origin header
# ALLOW_NULL_ORIGIN=false

# MongoDB Atlas connection
MONGODB_URI=mongodb+srv://<db_username>:<db_password>@cluster0.x2amlgq.mongodb.net/academiazen?retryWrites=true&w=majority

# Firebase Admin credentials (choose ONE method)
# Method A: Service account JSON (entire JSON string)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# Method B: Individual fields
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Optional: restrict admin-only endpoints
# ADMIN_EMAILS=admin1@example.com,admin2@example.com

# Gemini AI (server-side)
GEMINI_API_KEY=your_gemini_key
GEMINI_MODEL=gemini-1.5-flash

# PayMongo (billing)
# Secret key (sk_live_... or sk_test_...)
PAYMONGO_SECRET_KEY=
# Optional webhook signing secret (if configured in PayMongo dashboard)
PAYMONGO_WEBHOOK_SECRET=
# Optional override base URL
# PAYMONGO_API_BASE=https://api.paymongo.com/v1
# Optional override checkout redirect URLs
# PAYMONGO_SUCCESS_URL=https://academiazen.app/?billing=success
# PAYMONGO_CANCEL_URL=https://academiazen.app/?billing=cancel
